extends ../layouts/application.pug

block header
  = t('views.task.new.title')

//- block content
//-   +formFor()(action=route('tasks') method='post')
//-     +input(task, 'name', errors)(type="text")
//-     +input(task, 'description', errors)(type="text")
//-     +input(task, 'email', errors)(type="text")
//-     +input(user, 'password', errors)(type="password")
//-     +submit(user)(value=t('views.user.new.submit'))